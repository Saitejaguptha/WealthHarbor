import { SECTORS, MARKET_CAPS } from '../../types/stock';

export const ALL_OPTION = 'All';

export const DEFAULT_FILTERS = {
    searchTerm: '',
    selectedCap: ALL_OPTION,
    selectedSector: ALL_OPTION
};

export { SECTORS, MARKET_CAPS };
